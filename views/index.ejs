<!DOCTYPE html>
<html lang="en">

<%- include('./components/head.ejs') %>

<body>
    <%- include('./components/navbar.ejs') %>

    <%- "<h1>HOME HELLO WORLD</h1>" %>
    <% if(blogs.length) { %>
        <% blogs.forEach(blog => {  %>
            <h2 ><%= blog.title %></h2>
            <h3><%= blog.snippet %> ... </h3>
            <br><hr>
        <% }) %>
    <% } else { %>
        <h2>no blog found</h2>
    <% } %>

    <p>this is html file</p>


    
</body>
</html>